.vimeo-video-container.row(id="videos")
  .video.col(v-for="video in videos")
    h3 {{video.name}}
    .videoWrapper
      iframe(:src="`${video.url}?title=0&byline=0&portrait=0&sidedock=0`" frameborder="0" webkitallowfullscreen mozallowfullscreen allowfullscreen)
